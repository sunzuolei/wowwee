set(LIBRARY_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/lib)
set(EXECUTABLE_OUTPUT_PATH ${CMAKE_SOURCE_DIR}/testbin)
LINK_DIRECTORIES(${LIBRARY_OUTPUT_PATH})

ADD_SUBDIRECTORY(./robotdriver)

#OpenCV
#FIND_PACKAGE(OpenCV REQUIRED)
#FIND_PACKAGE(JPEG REQUIRED)

#INCLUDE_DIRECTORIES(./robotdriver/include ${OpenCV_INCLUDE_DIRS} ${JPEG_INCLUDE_DIR})
#AUX_SOURCE_DIRECTORY(./robotdriver/src ROBOT_LIB_CPP)
#list(APPEND LIB_CPP ${ROBOT_LIB_CPP} ${JPEG_LIBRARIES})
#MESSAGE(STATUS "LIB_CPP:" ${LIB_CPP})
#ADD_LIBRARY(robotdriver ${LIB_CPP})

#LINK_DIRECTORIES(${LIBRARY_OUTPUT_PATH})
#AUX_SOURCE_DIRECTORY(./test TEST_CPP)
#ADD_EXECUTABLE(testrobotobstacle ${TEST_CPP})
#TARGET_LINK_LIBRARIES(testrobotobstacle robotdriver ${OpenCV_LIBS} )
